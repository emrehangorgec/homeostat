<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!--
        testOnly keeps Device Owner removable during development:
        adb shell dpm remove-active-admin com.homeostat.agent/.AdminReceiver
        Install with: adb install -t
    -->
    <application
        android:label="homeostat"
        android:networkSecurityConfig="@xml/network_security_config"
        android:testOnly="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Own process: a kiosk crash must not take the admin receiver or guardian with it. -->
        <activity
            android:name=".KioskActivity"
            android:exported="true"
            android:process=":kiosk"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <receiver
            android:name=".AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- DUMP is held by the adb shell and unavailable to ordinary apps. -->
        <receiver
            android:name=".ProbeReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP" />

        <!-- Health contract v1 commands, addressed by package: am broadcast -p <pkg> -a ... -->
        <receiver
            android:name=".KioskControlReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:process=":kiosk">
            <intent-filter>
                <action android:name="com.homeostat.contract.RELOAD" />
                <action android:name="com.homeostat.contract.RESET_SESSION" />
            </intent-filter>
        </receiver>

        <!-- RESTART runs in the main process: it must work while the kiosk process is hung. -->
        <receiver
            android:name=".RestartReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.homeostat.contract.RESTART" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".DebugFaultReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:process=":kiosk" />
    </application>
</manifest>
